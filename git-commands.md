i# Git Commands Reference

---

# Setup & Config

## git --version

Shows installed Git version.

Example:

```bash
git --version
```

---

## git config --global user.name

Sets Git username globally.

Example:

```bash
git config --global user.name "Avinash S Kumar"
```

---

## git config --global user.email

Sets Git email globally.

Example:

```bash
git config --global user.email "example@gmail.com"
```

---

# Basic Workflow

## git init

Creates a new Git repository.

Example:

```bash
git init
```

---

## git add

Stages files for commit.

Example:

```bash
git add git-commands.md
```

---

## git commit

Creates a snapshot of staged changes.

Example:

```bash
git commit -m "Add git commands reference"
```

---

# Viewing Changes

## git status

Shows repository status.

Example:

```bash
git status
```

---

## git log

Shows commit history.

Example:

```bash
git log
```

## git diff

Shows changes between versions.

Example:

```bash
git diff


---

## Check Changes

```bash
git diff
